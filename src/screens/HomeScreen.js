import HomeContent from "../components/home/HomeContent";

/** Accueil branché API — skeleton géré dans HomeContent via TanStack Query. */
export default function HomeScreen() {
  return <HomeContent />;
}
