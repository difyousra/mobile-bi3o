Bi3oo SVG icons

Place all .svg files inside: src/assets/icons/
Place AppIcon.jsx inside: src/components/common/AppIcon.jsx

Required dependencies:
npx expo install react-native-svg
npm install react-native-svg-transformer

After updating metro.config.js, restart Expo:
npx expo start -c
